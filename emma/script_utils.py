def normalize_script(script: str) -> str:
    """Placeholder implementation that returns the script unchanged.
    In the full system this would perform cleanup, whitespace handling, and
    any domain‑specific normalisation required before TTS processing.
    """
    return script
